# /etc/audit/auditd.conf

local_events = yes
write_logs = yes
log_file = /var/log/audit/audit.log
log_group = adm
log_format = RAW

flush = INCREMENTAL_ASYNC
freq = 50

# Log rotation
max_log_file = 100              # 100MB before rotating
num_logs = 500                 	# Keep 100 rotated logs
max_log_file_action = ROTATE	# 50GB total log 

# Alerting
space_left = 100			# Alert at 100MB remaining
space_left_action = EMAIL
action_mail_acct = adresse@mail.com	# Change This

verify_email = yes
admin_space_left = 50          # SUSPEND at 50MB remaining
admin_space_left_action = SUSPEND
disk_full_action = HALT
disk_error_action = HALT

priority_boost = 5
name_format = HOSTNAME

# Network logging (disabled by default)
distribute_network = no

# Configure if you allow network logging
#use_libwrap = yes
#transport = TCP
#tcp_listen_queue = 5
#tcp_max_per_addr = 1
#tcp_client_max_idle = 60

q_depth = 2000
overflow_action = SYSLOG
max_restarts = 10

plugin_dir = /etc/audit/plugins.d
end_of_event_timeout = 2


# /etc/audit/auditd.conf

local_events = yes
write_logs = yes
log_file = /var/log/audit/audit.log
log_group = adm
log_format = RAW

flush = INCREMENTAL_ASYNC
freq = 50

# Log rotation
# 100MB before rotating
max_log_file = 100
# Keep 100 rotated logs
num_logs = 500
# 50GB total log
max_log_file_action = ROTATE

# Alerting
# Alert at 100MB remaining
space_left = 100
space_left_action = EMAIL
# Change This
action_mail_acct = adresse@mail.com

verify_email = yes
# SUSPEND at 50MB remaining
admin_space_left = 50
admin_space_left_action = SUSPEND
disk_full_action = HALT
disk_error_action = HALT

priority_boost = 5
name_format = HOSTNAME

# Network logging (disabled by default)
distribute_network = no

# Configure if you allow network logging
#use_libwrap = yes
#transport = TCP
#tcp_listen_queue = 5
#tcp_max_per_addr = 1
#tcp_client_max_idle = 60

q_depth = 2000
overflow_action = SYSLOG
max_restarts = 10

plugin_dir = /etc/audit/plugins.d
end_of_event_timeout = 2
